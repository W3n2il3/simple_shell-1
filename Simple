#include "shell"

/**
 * main -handle arguments
 *@Agrgc: the number of arguments
 *@Argv: array of strength 
 * Return : 0

int main(int argc, char *argv[])
{
int z;

for(z = 0, z < argc; z++)
    printf("argv[%d]=%s\n", z, argv[z]);
return(0);
